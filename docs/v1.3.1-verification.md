# v1.3.1 节次时间语义回归验证

## 状态

- Ethan 已完成 Windows 10 最终安装态人工验收，结论：**ALL PASS**。当前正在准备正式发布；v1.3.0 tag / GitHub Release 保持不变。
- 本验证文档提交后将以 `v1.3.1` tag 发布；不得修改或移动 `v1.3.0` tag。
- SQLite schema 保持 5；本轮没有 migration，也没有批量改写课程数据。
- 自动化回归和当前 v1.3.1 生产构建已通过；完整记录见本文后续章节。
- 验收过程不依赖开发机 AppData 数据替代安装态验证，用户数据库和原始课表 PDF 不进入仓库或安装包。

## 冻结的时间语义

- `startPeriod` 与 `endPeriod` 同时存在：`PERIOD_BASED`。唯一时间事实是星期、节次、周次；当前实际钟点只能由节次索引和当前已确认的 `PeriodTime[]` 解析。
- 两个节次索引同时为空：`FIXED_TIME`。此时 `startTime/endTime` 才是实际时间。
- 两个索引不成对：数据无效，不能默认为固定钟点。
- 为兼容 schema 5 和既有行，`PERIOD_BASED` Course 仍保存 `startTime/endTime` 快照；运行时绝不以其回退。作息缺失或节次无法映射时不显示旧钟点、不生成基础 occurrence，但保留课程原行，待作息有效后可再次解析。
- 手动固定钟点课程不随作息变化；明确钟点的 RESCHEDULE / MAKEUP 保持固定；仅换教室与停课仍作用于当前解析出的基础 occurrence。

## 数据与隐私边界

- SQLite schema 保持 5；本次没有 migration、课程批量改写或对用户数据库的修改。
- 回归通过隔离测试数据验证 `start_period/end_period` 持久化和作息变更后即时解析；本仓库不保存用户数据库、私有课表内容或 PDF 原文。
- Windows 10 安装态结果来自 Ethan 的实际验收，不以开发机 AppData 数据替代。

## 端到端数据路径核对

```text
PDF coordinate parser
→ ImportCandidate.weekday/startPeriod/endPeriod/weeks
→ evaluateImportCandidates / prepareCourseProposal
→ materializeImportCourses（保留候选节次索引）
→ import_courses
→ SQLite start_period/end_period
→ row_to_course / load_courses
→ getCourseTimeMode + resolveCourseTime
→ CourseOccurrence / timetable / Today / Widget / Reminder
```

- `src/importers/ntu-pdf/parse.ts` 识别星期/节次/周次，不根据节次生成时钟事实。
- `src/core/import-proposal.ts` 只用已确认作息将候选暂时解析成导入兼容钟点，并在最终 materialize 时重新校验且保留 `startPeriod/endPeriod`。
- Rust `import_courses`、SELECT 和 `row_to_course` 均持久化并回读 `start_period/end_period`；新增隔离 SQLite 测试覆盖导入、换作息、关闭重开后节次索引仍在且课程行未被改写。
- 生产展示的时间来自 `src/core/period-time.ts::resolveCourseTime`。`App.tsx` 在课程/提醒路径将结果合并为有效数据；`course-occurrence.ts::baseOccurrence` 只以该 resolver 产生 occurrence；Widget 同样解析后再构建视图。`CourseCard` 的节次标签直接显示课程节次索引，不再通过钟点反推。
- 作息保存更新 `periods` state，触发 `effectiveUserCourses` 与 canonical occurrence 重算；无有效作息的 PERIOD_BASED 课程不进入可显示列表。没有作息保存路径遍历或 UPDATE 全部课程。

## 回归覆盖

- `tests/unit/period-time.test.mjs`：节次课程忽略过期快照，以当前作息解析；作息缺失/映射不完整时返回 unresolved，而非快照回退；固定钟点课程保持原钟点；时间语义通过集中 helper 判定。
- `tests/unit/course-occurrence.test.mjs`：schedule/Today/Widget/Reminder canonical occurrences 随作息变化；固定时间不动；换教室/停课按新基础时段；明确调课时间固定；无法解析的节次课程不生成基础 occurrence。
- `tests/unit/import-proposal.test.mjs`：PDF 候选经过提案与 materialize 后保留节次索引；test-only 作息不得生成正式提案。
- `tests/ui/timetable.spec.ts`：通过持久化课程 fixture 连续将 P1 从 08:00 改为 07:50、08:10、07:40，主课表随即更新，且 Course 兼容快照不被改写、`update_course` 调用为 0；无确认作息时快照不会显示。真实本地 PDF 实际解析到 20 个候选（17 固定、3 非固定实践）；固定候选保留节次，3 条实践继续 blocking 并取消，不伪造星期/节次/周数。此前把实践数据硬填后导入的旧 UI 测试已移除这种错误假设。
- `src-tauri/src/db.rs`：隔离 SQLite 导入 / 作息修改 / reopen 回归，确认 `start_period/end_period` 保留且作息写入不改 Course 行。
- 本轮 targeted 验证：TypeScript typecheck PASS；相关单元测试 39/39 PASS；Playwright 目标用例 19 PASS、8 个仅运行一次的其他 viewport 组合按配置跳过；私有本地 PDF 用例在主 viewport 执行并 PASS。
- `npm run verify`：PASS；typecheck、132 项 unit tests、60 项 architecture tests、Playwright 453 PASS / 15 条件跳过、lint、Prettier 与 frontend build 均通过。
- Rust：`cargo test` 40/40 PASS；`cargo fmt -- --check` PASS；`cargo clippy --all-targets -- -D warnings` PASS。

## 构建及人工验收门禁

- 旧表中的 EXE/MSI/NSIS 与 `.sig` 哈希属于较早构建，已由以下当前源码产物取代。生产 build 使用当前工作树重新生成，EXE ProductVersion/FileVersion 均为 1.3.1；MSI 与 NSIS updater signatures 生成成功。

| 工件 | 大小 | SHA-256 |
|---|---:|---|
| `src-tauri/target/release/ntu-course-assistant.exe` | 66,010,624 bytes | `0AB6F935BDBA534B9A6445B47AD2A0960ED0BA5321A7DEC0D66321EFEC9A7CE3` |
| `NTU Course Assistant_1.3.1_x64_en-US.msi` | 55,017,472 bytes | `0322EBF102D55AC9D43A5001D4C9DF074DB395341A00A6937C929C9947BB7BC1` |
| `NTU Course Assistant_1.3.1_x64_en-US.msi.sig` | 436 bytes | `261C62D6DDAAA7EBADEB9B795232AFFE7E95B6F64A5FC71192B49B5DF0863C74` |
| `NTU Course Assistant_1.3.1_x64-setup.exe` | 52,037,405 bytes | `6456B71686DD3BFF163A656A7091A347AB17DB9C1E45D66F1CFC847699F2C675` |
| `NTU Course Assistant_1.3.1_x64-setup.exe.sig` | 436 bytes | `385C61B63A4878300B75F14815948DCBD9F3625486444FD7C0B080BE203D794A` |

- 上表为本地生产构建的 RC 工件记录；不会将本地构建目录或安装包提交到 Git。GitHub Release 将由 `v1.3.1` tag 对应的 Windows workflow 重新构建。
- Ethan 已在 Windows 10 安装态完成最终人工验收，结果 **ALL PASS**：节次课程随作息变更即时更新且无需重导入/重启；连续修改、多视图刷新、提醒、Widget、重启恢复均正确；固定钟点课程不变；换教室覆盖保留地点并跟随新作息时间；明确调课时间和停课状态保持正确。
- 本地自动验证：`npm run verify`（132 unit、60 architecture、453 UI PASS / 15 条件跳过）PASS；Rust 40 tests、`cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings` PASS。生产 EXE、MSI、NSIS 与 updater `.sig` 均由当前 v1.3.1 代码生成。
- 正式 GitHub tag、Actions 运行、云端 Release assets 和 `latest.json` 状态在发布后追加记录；当前尚未发布。
