# v1.2.0 Release Candidate 验证记录

- 状态：Release Candidate 准备中，等待最终 Windows 安装态人工验收。
- 版本：1.2.0；SQLite schema 保持 4。

## 已完成

- 官方 Tauri 2 Updater 已启用：HTTPS GitHub Releases endpoint、`passive` Windows install mode、启动后非阻塞检查、关于页手动检查、更新说明、下载进度、失败重试和 Release 页面回退。
- 正式 updater 公钥已配置；生产私钥仅保存在仓库外。已验证本机 NSIS 与 MSI bundle 及对应 `.sig` 文件存在。
- Release workflow 使用 GitHub Actions secrets `TAURI_SIGNING_PRIVATE_KEY` 和 `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`，并上传 updater JSON 与签名；不包含明文密钥。
- 课表支持“回到现在”、持久化 5/7 天偏好，以及 PDF 导入实际结果汇总。
- Settings / Schedule / Widget stability refactor (`e4d38bb`) 和 widget drag area (`d106720`) 保持冻结。
- Backup / Restore 已从产品、Rust commands、设置界面、filesystem capability、测试和文档中移除；SQLite schema 仍为 4。

## Course mutation stability

- 课程新增、编辑、删除、批量 PDF 导入、清空全部课程和作息/视图保存均通过 `spawn_blocking` 存储边界，避免同步数据库命令占用 Tauri runtime。
- 提醒刷新使用 generation 丢弃旧 mutation 的过期结果；空课程路径直接清空计划，不读取 handled history。
- Widget refresh 使用 single-flight/coalescing；刷新期间保留 last-known-good 课程内容，成功后原子发布新快照，失败显示错误状态而不白屏。
- 已保留 debug-only monotonic runtime trace，用于区分 mutation、DB、reminder、widget 和 event 阶段；不记录课程内容或密钥。

## PDF 文件选择器与设置保存并发回归

- 根因：`save_app_settings` 和 `patch_widget_settings` 是同步 Tauri command；SQLite 忙碌或等待锁时，它们会占用 Tauri 的命令处理线程，使本应独立的原生 dialog 调用不能及时获得运行时响应。
- 修复：这两个写入 command 现在只将既有 SQLite 操作调度到 Tauri blocking worker；数据库仍使用同一个受保护连接，事务与设置语义不变，且窗口 API 仍在释放数据库锁后执行。
- PDF 路径已独立复核：主窗口按钮仅在实际读取 PDF 时禁用；`choosePdfFile` 直接调用 `plugin:dialog|open`，随后才读取文件并进入 PDF.js/OCR 解析。设置遮罩仅在设置对话框可见时作为有意的 modal backdrop 存在，不存在隐藏的全局 loading backdrop。
- 新 UI 回归覆盖：正常 dialog invoke、widget 保存 pending、作息保存 pending、两项保存并发、保存失败后恢复可用，以及关闭设置后可直接点击导入。9 个 DPI/窗口组合均 PASS。

## 自动验证

- TypeScript targeted unit/architecture：PASS。
- 相关 UI mock 已更新以覆盖持久化 day count；27 个受影响 UI 场景：PASS。
- PDF/dialog 与 pending settings-save 回归：9 个 DPI/窗口组合 PASS。
- `npm run typecheck`：PASS；Rust `cargo test`：36 PASS；`cargo fmt --check`：PASS；`cargo clippy --all-targets -- -D warnings`：PASS。
- 本地正式构建已由用户完成；确认产物：
  - `src-tauri/target/release/bundle/msi/NTU Course Assistant_1.2.0_x64_en-US.msi`
  - 对应 `.sig`
  - `src-tauri/target/release/bundle/nsis/NTU Course Assistant_1.2.0_x64-setup.exe`
  - 对应 `.sig`

## 尚待人工 / GitHub 验证

- 使用本地安装包完成 Windows 安装态烟雾验收。
- 真实 GitHub Draft Release 的 `latest.json`、签名下载与跨版本 updater E2E，须在用户允许创建 v1.2.0 tag 后执行；当前 RC 阶段禁止创建 tag 或发布。
- 生产私钥和密码从未写入仓库、文档或日志。

## 本地最终构建

- `npm run verify`：PASS（UI 396 项全通过，112 项 unit，48 项 architecture）。
- `cargo test --manifest-path src-tauri/Cargo.toml`：PASS（36 tests）。
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`：PASS。
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`：PASS。
- `npm run tauri build`：PASS，生成当前 HEAD 的 v1.2.0 NSIS/MSI 及 `.sig`。

## 等待用户的 Windows GUI 验收

Computer Use 禁止；以下必须由用户手动完成：启动、Widget/作息保存、真实 PDF 导入后再次保存、重复保存、PDF 文件选择器、清空课程速度与空状态、Updater 错误弹窗重试/关闭、Widget show/hide、Tray true-exit 和重启恢复。Backup / Restore 已不再测试。
## 本地构建产物（不进入 Git）

| 文件 | 大小 | SHA-256 |
| --- | ---: | --- |
| `NTU Course Assistant_1.2.0_x64_en-US.msi` | 54,833,152 bytes | `E7332345024F0F37BE6AE9A78C33074C2FDBBB727B8E34CBC91EC462B22A6F28` |
| `NTU Course Assistant_1.2.0_x64_en-US.msi.sig` | 436 bytes | `0A0407639D872A14433C30C5AD7BCB337398D919C8C1E0FE9A4FFB12832BD037` |
| `NTU Course Assistant_1.2.0_x64-setup.exe` | 51,942,178 bytes | `9F631ABB2340FA0B834586296F05F25CF883BC9FD29E10766DF357A64CA71965` |
| `NTU Course Assistant_1.2.0_x64-setup.exe.sig` | 436 bytes | `BE5500BBFD66037F38007C31D1D03B0485BCF464A08A97716E1F75040DA2B926` |

## 最终一次 Windows 人工验收

1. 用 NSIS 安装包安装并启动；确认版本为 v1.2.0、主课表和 PDF/OCR 正常。
2. 点击“回到现在”；确认回到当前教学周并定位当前时间附近。
3. 在“关于”点击“检查更新”；无更新时出现明确提示，失败时仍可继续使用应用。
4. 打开 Widget、关闭主窗口到 Tray、从 Tray 恢复并最终选择“退出程序”；确认进程、Widget 和 Tray 均正常结束。
5. 确认课程提醒、作息保存和 Widget 保存没有回归。
6. 在 Widget 保存中、作息保存中，以及连续两类保存后分别关闭设置并点击“导入 PDF”；每次均确认 Windows PDF 文件选择器立即出现。保存失败后同样确认文件选择器可用。

真实 GitHub Draft Release 的 `latest.json`、下载签名校验与跨版本自动更新，必须在 RC 被接受、创建 v1.2.0 tag 后验证；当前阶段未创建 tag、未发布 Release。
