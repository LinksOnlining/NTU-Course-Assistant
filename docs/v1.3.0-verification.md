# v1.3.0 Academic Hub 验证记录

## 当前状态

当前为 Release Candidate，自动门禁与 v1.3.0 生产构建已通过；本轮 Academic Hub UI/交互重构已完成，未创建 `v1.3.0` tag，未发布 GitHub Release，等待 Ethan 完成最终 Windows 10 安装态人工验收。

## 已实现范围

- `CourseOccurrence` / `CourseOverride`：停课、调课、换教室、补课。
- `AcademicTask` / `Exam`：Deadline、完成状态、考试信息。
- `getTodayDashboard` 与统一 Reminder 计划。
- Academic Hub 的今日、课表变化、任务、考试、学期管理页面。
- Widget NEXT / TODAY / DEADLINES 模式。
- schema 4→5 非破坏 migration，旧课程和设置保持可读取。
- 周课表和 Widget 读取 canonical occurrence，变化状态在课程卡片中可见。

## 本轮 RC 阻断修复

- 新增 `ChineseDateInput` / `ChineseDateTimeInput`：Task、Exam、课程变化编辑器使用可见的“年/月/日”字段和日历按钮，向领域层仍输出标准 ISO 日期/日期时间；不再依赖 Windows WebView 的 `yyyy/mm/dd` 原生占位文本。
- 修复 Academic Hub 页面滚动：应用级 `body` 保持固定窗口布局，Hub 自身承担纵向滚动并隐藏横向溢出，课表变化列表可从首条滚动到末条。
- 修复停课显示：`CANCELLED` canonical occurrence 在正式周课表布局中被过滤，因此停课不会继续显示为正常课程；Today、Widget 和提醒继续消费同一 canonical occurrence 并排除取消后的下一节课。
- 修复补课与覆盖解析：`MAKEUP` 不再替换原始课程，撤销 active override 可恢复基础 occurrence，同一 occurrence 的最新 active override 优先。
- 修复课程变化编辑器：调课、换教室、补课改为每条课程独立的可用编辑表单，保存前执行目标日期/时间冲突确认；停课仍只影响选中的一次课程。
- 修复学习中心分区标签在 CSS Grid 中被纵向拉伸的问题：标签保持紧凑高度，窄窗口改为横向滚动，不压缩文字。
- 重构课表变化交互为“课程 → 发生次数 → 操作”：课程列表按稳定 `courseId` 去重，支持课程名/教师/教室搜索与“全部/有变化”筛选；详情保留全部发生次数、中文日期、变化状态、现有覆盖和撤销入口，补课从课程详情顶部进入。
- 课程变化操作继续通过 canonical occurrence/resolver 与既有 `onDataChanged` 刷新链路驱动课表、Today、Widget 和 Reminder，不在 UI 重算规则。

## 自动验证

| 检查 | 状态 |
| --- | --- |
| `npm run typecheck` | PASS（已执行） |
| `npm run test:unit` | PASS（128 项，已执行） |
| `npm run test:arch` | PASS（60 项，已执行） |
| `npm run lint` | PASS（已执行） |
| `npm run format:check` | PASS（已执行） |
| `cargo test --manifest-path src-tauri/Cargo.toml` | PASS（39 项） |
| `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` | PASS |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` | PASS |
| `npm run verify` | PASS（UI 450 项：435 通过、15 跳过；typecheck、unit、architecture、lint、format、build 均通过） |
| `npm run tauri build` | PASS（v1.3.0，NSIS/MSI 与 updater signatures 已重新生成） |

Rust 构建仍会输出 Windows linker 的 `linker_messages` 信息；这是已知的非阻断编译警告，不影响结果。

## v1.3.0 生产构建产物

构建目录：`src-tauri/target/release/bundle/`（构建产物保持 ignored，不提交 Git）。

| 产物 | 大小 | SHA-256 |
| --- | ---: | --- |
| `NTU Course Assistant_1.3.0_x64_en-US.msi` | 55,025,664 bytes | `A35A8F33EB7CD93688256738A81C8F6B051DB9C669E6736DB9F3DC1B6B8279E1` |
| `NTU Course Assistant_1.3.0_x64_en-US.msi.sig` | 436 bytes | `AE80B3E760FE469C83091DA96D91DF8020C9393FC940D658B695CF35904F5505` |
| `NTU Course Assistant_1.3.0_x64-setup.exe` | 52,043,165 bytes | `A5CE1A1D05388FBBF71F66B9EFBE3F84E37A7491C2521E1F84D9C19C1FCE7C2D` |
| `NTU Course Assistant_1.3.0_x64-setup.exe.sig` | 436 bytes | `3A48EF0BE2E236AB3A77B92870477E87DC74625495FC5AFFA9BFDD12D600E12B` |

签名文件与本次 2026-09-22 生产构建同时生成；私钥及密码未进入仓库或文档。

## Windows 10 验收

Codex 未代替用户执行 Windows GUI 操作。Ethan 需要使用上述最新 NSIS 安装包完成一次安装态验收，至少覆盖：

- 默认打开“今日”学习中心，并能返回周课表；
- 停课、调课、换教室、补课及撤销/冲突提示；
- 学习事项新增、编辑、完成、恢复、删除和截止日期倒计时；
- 考试新增、编辑、删除、课程关联和倒计时；
- Widget 的 TODAY / NEXT / DEADLINES、显示/隐藏、锁定和重启恢复；
- 变化/任务/考试更新后提醒计划重建；
- 学期归档、历史查看、恢复，以及 v1.2.1 数据启动兼容；
- Tray、single-instance、退出和重启后的设置/课程数据恢复。

在上述人工验收完成前，本阶段保持 RC，不创建 tag 或发布 Release。

## 已知兼容边界

- v1.2.1 以前没有 semesterId 的 legacy Course 按当前 active semester 兼容显示；新学期相关数据都带 semesterId。
- 应用完全退出后不运行提醒 scheduler；关闭到托盘时提醒继续。
- 不创建 v1.3.0 tag，不发布 Release，直到 Ethan 完成 Windows 10 RC 验收。
