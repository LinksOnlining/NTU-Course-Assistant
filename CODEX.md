# 项目开发规则

本项目由单一 Codex 负责分析、设计、编码、测试、修复、文档和 Git。用户本轮原始要求见 docs/requirements.md，优先于模板的历史协作安排。

开始前阅读 README.md、DESIGN.md、PROJECT_STATUS.md、CHANGELOG.md 和 docs/phase-0.md。

仅处理已获确认的当前阶段。小步实现 → 针对性验证 → 修复 → PASS → 更新文档 → Git 提交。每个大阶段完成后停止，等待用户确认下一阶段。

Phase 0 只做检查、设计、文档初始化；不创建应用代码，不安装未来模块依赖。Phase 1 必须获得用户确认。

core 保持纯逻辑，只依赖纯类型；UI 负责展示与交互；外部 IO 放在 adapters 或 Rust 边界。禁止提前创建大量空接口、插件框架或未来功能。

普通技术问题自主修复；重大架构变化、凭证、收费、重要数据删除和项目外重要修改再询问。不得保存学生密码，不绕过身份验证。

不得将未知课程字段填成猜测值。导入先预览，明确确认后才允许写入正式数据。

每次报告实际执行的检查。没有执行的构建、运行、测试标记 N/A 或未验证，不得宣称 PASS。阶段稳定后本地提交，不自动推送远端。
